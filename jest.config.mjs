export default {
  // Match test files under __tests__/ with .test.mjs or .test.js extensions.
  testMatch: [
    "**/__tests__/**/*.test.?(m)[jt]s?(x)",
  ],

  // Display individual test results with the test suite hierarchy.
  verbose: true,
}
